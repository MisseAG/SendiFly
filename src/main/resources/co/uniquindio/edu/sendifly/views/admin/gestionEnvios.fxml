<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<VBox spacing="15" styleClass="crud-container" xmlns="http://javafx.com/javafx"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="co.uniquindio.edu.sendifly.controllers.CrudShipmentsController">

    <padding>
        <Insets bottom="10" left="10" right="10" top="10" />
    </padding>

    <!-- Barra de Acciones -->
    <HBox alignment="CENTER_LEFT" spacing="10">
        <Button fx:id="btnCrear" onAction="#handleCrear" styleClass="action-button-create" text="➕ Crear Envío" />
        <Button fx:id="btnActualizar" disable="true" onAction="#handleActualizar" styleClass="action-button-update" text="✏️ Actualizar" />
        <Button fx:id="btnEliminar" disable="true" onAction="#handleEliminar" styleClass="action-button-delete" text="🗑️ Eliminar" />

        <Region HBox.hgrow="ALWAYS" />

        <TextField fx:id="txtBuscar" prefWidth="200" promptText="🔍 Buscar por ID..." styleClass="search-field" />
        <Button fx:id="btnBuscar" onAction="#handleBuscar" styleClass="action-button-search" text="Buscar" />
    </HBox>

    <!-- Tabla de Envíos -->
    <TableView fx:id="tablaEnvios" styleClass="data-table" VBox.vgrow="ALWAYS">
        <columns>
            <TableColumn fx:id="colId" prefWidth="100" text="ID" />
            <TableColumn fx:id="colOrigen" prefWidth="120" text="Origen" />
            <TableColumn fx:id="colDestino" prefWidth="120" text="Destino" />
            <TableColumn fx:id="colFechaPedido" prefWidth="120" text="Fecha Pedido" />
            <TableColumn fx:id="colFechaEntrega" prefWidth="160" text="Fecha Entrega" />
            <TableColumn fx:id="colPrecio" prefWidth="100" text="Precio" />
            <TableColumn fx:id="colEstado" prefWidth="120" text="Estado" />
        </columns>
        <columnResizePolicy>
            <TableView fx:constant="CONSTRAINED_RESIZE_POLICY" />
        </columnResizePolicy>
    </TableView>

    <!-- Información de Registros -->
    <HBox alignment="CENTER_LEFT" spacing="10">
        <Label fx:id="lblTotalRegistros" styleClass="info-label" text="Total de registros: 0">
            <font>
                <Font size="12.0" />
            </font>
        </Label>

        <Region HBox.hgrow="ALWAYS" />

        <Label fx:id="lblRegistroSeleccionado" styleClass="info-label-hint" text="Selecciona un envío para editar o eliminar">
            <font>
                <Font size="11.0" />
            </font>
        </Label>
    </HBox>

</VBox>
