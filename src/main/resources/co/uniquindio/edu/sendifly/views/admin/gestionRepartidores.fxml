<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<VBox spacing="15" styleClass="crud-container" xmlns="http://javafx.com/javafx/17.0.12" xmlns:fx="http://javafx.com/fxml/1" fx:controller="co.uniquindio.edu.sendifly.controllers.CrudDeliveriesController">

    <padding>
        <Insets bottom="10" left="10" right="10" top="10" />
    </padding>

    <!-- Barra de Acciones -->
    <HBox alignment="CENTER_LEFT" spacing="10">
        <Button fx:id="btnCrear" onAction="#handleCrear" styleClass="action-button-create" text="➕ Crear Repartidor" />
        <Button fx:id="btnActualizar" disable="true" onAction="#handleActualizar" styleClass="action-button-update" text="✏️ Actualizar" />
        <Button fx:id="btnEliminar" disable="true" onAction="#handleEliminar" styleClass="action-button-delete" text="🗑️ Eliminar" />

        <!-- Spacer -->
        <Region HBox.hgrow="ALWAYS" />

        <!-- Búsqueda -->
        <TextField fx:id="txtBuscar" prefWidth="200" promptText="🔍 Buscar..." styleClass="search-field" />
        <Button fx:id="btnBuscar" onAction="#handleBuscar" styleClass="action-button-search" text="Buscar" />
    </HBox>

    <!-- Tabla de Usuarios -->
    <TableView fx:id="tablaRepartidores" styleClass="data-table" VBox.vgrow="ALWAYS">
        <columns>
            <TableColumn fx:id="colId" prefWidth="60" styleClass="table-column-id" text="ID" />
            <TableColumn fx:id="colNombreCompleto" prefWidth="200" styleClass="table-column-text" text="Nombre Completo" />
            <TableColumn fx:id="colCorreo" prefWidth="220" styleClass="table-column-text" text="Correo Electrónico" />
            <TableColumn fx:id="colTelefono" prefWidth="120" styleClass="table-column-text" text="Teléfono" />
            <TableColumn fx:id="colDisponibilidad" prefWidth="150" styleClass="table-column-text" text="Disponibilidad" />
        </columns>
        <columnResizePolicy>
            <TableView fx:constant="CONSTRAINED_RESIZE_POLICY" />
        </columnResizePolicy>
    </TableView>

    <!-- Información de Registros -->
    <HBox alignment="CENTER_LEFT" spacing="10">
        <Label fx:id="lblTotalRegistros" styleClass="info-label" text="Total de registros: 0">
            <font>
                <Font size="12.0" />
            </font>
        </Label>

        <!-- Spacer -->
        <Region HBox.hgrow="ALWAYS" />

        <Label fx:id="lblRegistroSeleccionado" styleClass="info-label-hint" text="Selecciona un registro para editar o eliminar">
            <font>
                <Font size="11.0" />
            </font>
        </Label>
    </HBox>

</VBox>
